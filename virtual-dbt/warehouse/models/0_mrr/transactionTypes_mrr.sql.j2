{{
  config(
    materialized = 'table',
    )
}}
with transactionTypes as (
    select 
        tt.TransactionTypeID
        ,tt.transactionTypeName
    from {{ source('Application', 'TransactionTypes') }} as tt
)
select *
from transactionTypes